// TODO: Translate old condition formats to new ones, mostly pokemon_matcher based.
const modernizers = {
  ability: () => ({

  })
}

export function crawlForConditions(old) {
  // TODO: Crawl through the old object and translate conditions to new format.
}